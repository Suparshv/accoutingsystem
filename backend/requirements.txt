# Urban Furniture Accounting — backend dependencies.
# Versions are pinned exactly per SPEC.md §3. Do not use latest, ^ or ~.
# If a pin fails to install, report it and stop — do not silently upgrade.

# --- framework ---
fastapi==0.115.6
uvicorn==0.34.0

# --- orm and db ---
sqlalchemy==2.0.36
psycopg[binary]==3.2.3

# --- validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- auth ---
pyjwt==2.10.1
bcrypt==4.2.1

# --- utils ---
python-multipart==0.0.20

# --- dev ---
pytest==8.3.4
httpx==0.28.1
ruff==0.8.4
